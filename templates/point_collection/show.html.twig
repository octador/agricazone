{% extends 'base.html.twig' %}

{% block title %}PointCollection{% endblock %}

{% block body %}
    <h1>PointCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ point_collection.id }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ point_collection.adress }}</td>
            </tr>
            <tr>
                <th>Postalcode</th>
                <td>{{ point_collection.postalcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ point_collection.city }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ point_collection.description }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ point_collection.createdAt ? point_collection.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_point_collection_index') }}">back to list</a>

    <a href="{{ path('app_point_collection_edit', {'id': point_collection.id}) }}">edit</a>

    {{ include('point_collection/_delete_form.html.twig') }}
{% endblock %}
